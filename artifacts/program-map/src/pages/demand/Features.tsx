import { PageShell } from '@/components/platform/PageShell';
export default function Features() {
  return (
    <PageShell
      section="Demand Management"
      title="Features"
      badge="future-state"
      subtitle="Discrete deliverable features broken out from epics. Prioritized by sprint and mapped to programs or Trail OS capabilities."
      integration="GitHub Projects, Salesforce"
    />
  );
}

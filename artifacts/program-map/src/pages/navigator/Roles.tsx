import { PageShell } from '@/components/platform/PageShell';

export default function Roles() {
  return (
    <PageShell
      section="Navigator"
      title="Roles"
      badge="future-state"
      subtitle="Organizational roles and responsibilities across Transition Trails programs, RESOLVE methodology, and Trail OS operations."
      integration="Salesforce contacts, internal HR directory"
    />
  );
}

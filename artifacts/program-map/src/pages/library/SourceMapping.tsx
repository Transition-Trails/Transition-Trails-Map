import { PageShell } from '@/components/platform/PageShell';
export default function SourceMapping() {
  return (
    <PageShell
      section="Knowledge Library"
      title="Source Mapping"
      badge="future-state"
      subtitle="Visual mapping of which source documents are authoritative for which decisions, programs, and knowledge domains."
      integration="Internal knowledge graph (future)"
    />
  );
}

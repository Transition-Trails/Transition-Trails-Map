import { PageShell } from '@/components/platform/PageShell';
export default function Templates() {
  return (
    <PageShell
      section="Knowledge Library"
      title="Templates"
      badge="future-state"
      subtitle="Reusable templates for RESOLVE phases, program planning, facilitation guides, and client deliverables — synced from Google Drive."
      integration="Google Drive + Google Docs API"
    />
  );
}

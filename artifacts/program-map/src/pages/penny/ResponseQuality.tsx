import { PageShell } from '@/components/platform/PageShell';
export default function ResponseQuality() {
  return (
    <PageShell
      section="Penny Command Center"
      title="Response Quality"
      badge="future-state"
      subtitle="Quality scoring, flagged responses, blocked phrase management, and improvement workflows for Penny's AI outputs."
      integration="Agentforce response scoring API"
    />
  );
}
